# render.yaml — one-click Render Blueprint: web service + Postgres database.
services:
  - type: web
    name: project_3_ecommerce_store
    runtime: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn shopcart.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: project_3_ecommerce_store-db
          property: connectionString

databases:
  - name: project_3_ecommerce_store-db
    plan: free
