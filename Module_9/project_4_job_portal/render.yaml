# render.yaml — one-click Render Blueprint: web service + Postgres database.
services:
  - type: web
    name: project_4_job_portal
    runtime: python
    plan: free
    buildCommand: "./build.sh"
    startCommand: "gunicorn careerhub.wsgi:application"
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: ".onrender.com"
      - key: DATABASE_URL
        fromDatabase:
          name: project_4_job_portal-db
          property: connectionString

databases:
  - name: project_4_job_portal-db
    plan: free
