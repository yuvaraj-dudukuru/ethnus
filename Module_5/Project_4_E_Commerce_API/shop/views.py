# ============================================================================
#  views.py — intentionally (almost) empty
# ----------------------------------------------------------------------------
#  Django creates this file by default for ordinary HTML views. Our project is
#  an API, so all the real logic lives in api_views.py instead. We keep this
#  file so the app structure stays standard, but there is nothing to do here.
# ============================================================================
